# React example

React 18 host that mounts `createThreeViewport` against a `ModelingSession`.

```bash
pnpm --filter @modeling-kit/example-react dev
```

Call `viewport.dispose()` and `editor.dispose()` from the component cleanup. See [`docs/guides/examples.md`](../../docs/guides/examples.md).
