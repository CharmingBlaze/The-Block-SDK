# Playground

Interactive Three.js host for `@modeling-kit/sdk`: primitives, extrusion, undo/redo, and glTF export.

```bash
pnpm --filter @modeling-kit/playground dev
```

Dispose the viewport, then the session, on unmount. See [`docs/guides/examples.md`](../../docs/guides/examples.md).
