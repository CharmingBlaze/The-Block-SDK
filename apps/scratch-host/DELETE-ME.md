# Disposable scratch host

This app is a throwaway viewport loop for modeling-kit. It is **not** a product surface.

Delete the entire `apps/scratch-host` folder when you no longer need it. Nothing else in the repo depends on it.

Run: `pnpm --filter @modeling-kit/scratch-host dev`
