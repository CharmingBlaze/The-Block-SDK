# R1-T004 — Dissolve vertex/face, collapse edge, reverse winding

**State:** INTEGRATED  
**Owner:** Cursor  
**Requirements:** MESH-OP-023, MESH-OP-024, MESH-OP-027  
**Spec:** `docs/architecture/modeling-operator-specification.md` §5.4

## Goal

Public `MeshOperationResult` operators for dissolve vertex, dissolve face (region merge with duplicate/entire-mesh rejection), collapse edge (via `mergeVertices` center), and reverse face winding.

## Implementation

- `packages/mesh/src/operations/dissolve-collapse.ts`
- Commands: `packages/commands/src/dissolve-collapse.ts`
- Tests: `packages/mesh/tests/dissolve-collapse.test.ts`; command undo in `commands.test.ts`

## Evidence

```text
pnpm test       # 49 files, 268 passed
pnpm typecheck  # passed
```

Cube `dissolveFace` is rejected when it would duplicate the opposite face. 2x2 grid corner dissolve is the happy path.
